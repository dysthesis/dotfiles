use std::fs::read_to_string;

fn main() {
    let file = read_to_string("./main.rs");
    for line in file {
        println!(line);
        for elem in line.chars() {
            println!("{elem} from line {line}");
        }
    }
}
